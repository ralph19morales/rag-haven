# --- Core RAG stack (all local / offline-capable) ---
chromadb>=0.5.0              # local persistent vector store
sentence-transformers>=3.0.0 # local embeddings (bge models)
rank-bm25>=0.2.2            # keyword/lexical retrieval for hybrid search
ollama>=0.3.0              # client for the local Ollama LLM server

# --- Document loading ---
pypdf>=4.2.0              # PDF text extraction (fast path)
pdfplumber>=0.11.0        # PDF fallback for tricky layouts
pypdfium2>=4.30.0         # render PDF pages to images for OCR (no Poppler)
pytesseract>=0.3.10       # OCR for scanned PDFs (needs Tesseract binary)
python-docx>=1.1.0        # .docx files
beautifulsoup4>=4.12.0    # HTML parsing
lxml>=5.2.0              # fast HTML/XML backend

# --- Fetching official sources ---
requests>=2.32.0
httpx>=0.27.0

# --- Interfaces ---
streamlit>=1.36.0        # web chat UI
rich>=13.7.0             # pretty CLI output

# --- Utilities ---
python-dotenv>=1.0.0
tqdm>=4.66.0
